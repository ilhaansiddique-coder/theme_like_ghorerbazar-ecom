{
  "name": "shopkart-theme",
  "private": true,
  "description": "Theme source. `npm run dev` here launches the sibling preview app (cd-ecom-theme-preview) on http://localhost:3100. This file is NOT needed when copying the theme into the real engine — copy only pages/, chrome/, components/, manifest.ts and styles.css.",
  "scripts": {
    "dev": "bash ../run-theme-preview.sh",
    "preview": "bash ../run-theme-preview.sh",
    "build": "cd ../cd-ecom-theme-preview && npm run build"
  }
}
